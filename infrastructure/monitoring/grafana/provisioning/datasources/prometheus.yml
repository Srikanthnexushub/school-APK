apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:${PROMETHEUS_PORT:-9090}
    isDefault: true
    jsonData:
      timeInterval: ${PROMETHEUS_SCRAPE_INTERVAL:-15s}
