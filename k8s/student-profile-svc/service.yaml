apiVersion: v1
kind: Service
metadata:
  name: student-profile-svc
  namespace: edutech
  labels:
    app: student-profile-svc
    app.kubernetes.io/name: student-profile-svc
    app.kubernetes.io/part-of: edutech-platform
spec:
  selector:
    app: student-profile-svc
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      protocol: TCP
  type: ClusterIP
