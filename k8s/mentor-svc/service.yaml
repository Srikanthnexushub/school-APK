apiVersion: v1
kind: Service
metadata:
  name: mentor-svc
  namespace: edutech
  labels:
    app: mentor-svc
    app.kubernetes.io/name: mentor-svc
    app.kubernetes.io/part-of: edutech-platform
spec:
  selector:
    app: mentor-svc
  ports:
    - name: http
      port: 8088
      targetPort: 8088
      protocol: TCP
    - name: grpc
      port: 9098
      targetPort: 9098
      protocol: TCP
  type: ClusterIP
