apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: edutech
  labels:
    app: auth-svc
    app.kubernetes.io/name: auth-svc
    app.kubernetes.io/part-of: edutech-platform
spec:
  selector:
    app: auth-svc
  ports:
    - name: http
      port: 8182
      targetPort: 8182
      protocol: TCP
  type: ClusterIP
