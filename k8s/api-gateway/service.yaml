apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: edutech
  labels:
    app: api-gateway
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: edutech-platform
spec:
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8180
      targetPort: 8180
      protocol: TCP
  type: ClusterIP
