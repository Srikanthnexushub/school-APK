apiVersion: apps/v1
kind: Deployment
metadata:
  name: career-oracle-svc
  namespace: edutech
  labels:
    app: career-oracle-svc
    app.kubernetes.io/name: career-oracle-svc
    app.kubernetes.io/part-of: edutech-platform
    app.kubernetes.io/component: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: career-oracle-svc
  template:
    metadata:
      labels:
        app: career-oracle-svc
        app.kubernetes.io/name: career-oracle-svc
        app.kubernetes.io/part-of: edutech-platform
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8087"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: career-oracle-svc
          image: "$(DOCKER_REGISTRY)/$(DOCKER_IMAGE_PREFIX)/career-oracle-svc:$(IMAGE_TAG)"
          imagePullPolicy: Always
          ports:
            - containerPort: 8087
              name: http
              protocol: TCP
            - containerPort: 9097
              name: grpc
              protocol: TCP
          envFrom:
            - configMapRef:
                name: edutech-common-config
            - secretRef:
                name: edutech-secrets
          env:
            - name: SERVER_PORT
              value: "8087"
            - name: CAREER_ORACLE_SVC_PORT
              value: "8087"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: registry-credentials
