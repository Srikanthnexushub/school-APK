apiVersion: v1
kind: Service
metadata:
  name: center-svc
  namespace: edutech
  labels:
    app: center-svc
    app.kubernetes.io/name: center-svc
    app.kubernetes.io/part-of: edutech-platform
spec:
  selector:
    app: center-svc
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP
  type: ClusterIP
