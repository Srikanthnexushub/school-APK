apiVersion: v1
kind: Service
metadata:
  name: ai-gateway-svc
  namespace: edutech
  labels:
    app: ai-gateway-svc
    app.kubernetes.io/name: ai-gateway-svc
    app.kubernetes.io/part-of: edutech-platform
spec:
  selector:
    app: ai-gateway-svc
  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP
  type: ClusterIP
