# THIS IS A TEMPLATE ONLY — do NOT commit with real values.
# Supply secrets via one of these approaches:
#   Option A: kubectl create secret generic edutech-secrets --from-env-file=.env -n edutech
#   Option B: External Secrets Operator + HashiCorp Vault
#             (see k8s/external-secrets/ for ESO manifests)
#   Option C: AWS Secrets Manager / GCP Secret Manager via ESO
#
# Reference: https://external-secrets.io/latest/provider/hashicorp-vault/
apiVersion: v1
kind: Secret
metadata:
  name: edutech-secrets
  namespace: edutech
  annotations:
    # Annotate with Vault path if using External Secrets Operator
    # external-secrets.io/vault-path: "secret/data/edutech/production"
type: Opaque
stringData:
  # -------------------------------------------------------------------------
  # PostgreSQL — root admin (used by init jobs only)
  # -------------------------------------------------------------------------
  POSTGRES_ROOT_USER: "REPLACE_VIA_VAULT"
  POSTGRES_ROOT_PASSWORD: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Per-service database credentials
  # -------------------------------------------------------------------------
  AUTH_SVC_DB_NAME: "REPLACE_VIA_VAULT"
  AUTH_SVC_DB_USERNAME: "REPLACE_VIA_VAULT"
  AUTH_SVC_DB_PASSWORD: "REPLACE_VIA_VAULT"
  AUTH_SVC_DB_POOL_MAX_SIZE: "REPLACE_VIA_VAULT"
  AUTH_SVC_DB_POOL_MIN_IDLE: "REPLACE_VIA_VAULT"
  AUTH_SVC_DB_CONNECTION_TIMEOUT_MS: "REPLACE_VIA_VAULT"
  AUTH_SVC_DB_IDLE_TIMEOUT_MS: "REPLACE_VIA_VAULT"

  PARENT_SVC_DB_NAME: "REPLACE_VIA_VAULT"
  PARENT_SVC_DB_USERNAME: "REPLACE_VIA_VAULT"
  PARENT_SVC_DB_PASSWORD: "REPLACE_VIA_VAULT"
  PARENT_SVC_DB_POOL_MAX_SIZE: "REPLACE_VIA_VAULT"
  PARENT_SVC_DB_POOL_MIN_IDLE: "REPLACE_VIA_VAULT"
  PARENT_SVC_DB_CONNECTION_TIMEOUT_MS: "REPLACE_VIA_VAULT"
  PARENT_SVC_DB_IDLE_TIMEOUT_MS: "REPLACE_VIA_VAULT"

  CENTER_SVC_DB_NAME: "REPLACE_VIA_VAULT"
  CENTER_SVC_DB_USERNAME: "REPLACE_VIA_VAULT"
  CENTER_SVC_DB_PASSWORD: "REPLACE_VIA_VAULT"
  CENTER_SVC_DB_POOL_MAX_SIZE: "REPLACE_VIA_VAULT"
  CENTER_SVC_DB_POOL_MIN_IDLE: "REPLACE_VIA_VAULT"
  CENTER_SVC_DB_CONNECTION_TIMEOUT_MS: "REPLACE_VIA_VAULT"
  CENTER_SVC_DB_IDLE_TIMEOUT_MS: "REPLACE_VIA_VAULT"

  ASSESS_SVC_DB_NAME: "REPLACE_VIA_VAULT"
  ASSESS_SVC_DB_USERNAME: "REPLACE_VIA_VAULT"
  ASSESS_SVC_DB_PASSWORD: "REPLACE_VIA_VAULT"
  ASSESS_SVC_DB_POOL_MAX_SIZE: "REPLACE_VIA_VAULT"
  ASSESS_SVC_DB_POOL_MIN_IDLE: "REPLACE_VIA_VAULT"
  ASSESS_SVC_DB_CONNECTION_TIMEOUT_MS: "REPLACE_VIA_VAULT"
  ASSESS_SVC_DB_IDLE_TIMEOUT_MS: "REPLACE_VIA_VAULT"

  PSYCH_SVC_DB_NAME: "REPLACE_VIA_VAULT"
  PSYCH_SVC_DB_USERNAME: "REPLACE_VIA_VAULT"
  PSYCH_SVC_DB_PASSWORD: "REPLACE_VIA_VAULT"
  PSYCH_SVC_DB_POOL_MAX_SIZE: "REPLACE_VIA_VAULT"
  PSYCH_SVC_DB_POOL_MIN_IDLE: "REPLACE_VIA_VAULT"
  PSYCH_SVC_DB_CONNECTION_TIMEOUT_MS: "REPLACE_VIA_VAULT"
  PSYCH_SVC_DB_IDLE_TIMEOUT_MS: "REPLACE_VIA_VAULT"

  STUDENT_PROFILE_DB_NAME: "REPLACE_VIA_VAULT"
  STUDENT_PROFILE_DB_USER: "REPLACE_VIA_VAULT"
  STUDENT_PROFILE_DB_PASSWORD: "REPLACE_VIA_VAULT"

  EXAM_TRACKER_DB_NAME: "REPLACE_VIA_VAULT"
  EXAM_TRACKER_DB_USER: "REPLACE_VIA_VAULT"
  EXAM_TRACKER_DB_PASSWORD: "REPLACE_VIA_VAULT"

  PERFORMANCE_DB_NAME: "REPLACE_VIA_VAULT"
  PERFORMANCE_DB_USER: "REPLACE_VIA_VAULT"
  PERFORMANCE_DB_PASSWORD: "REPLACE_VIA_VAULT"

  AI_MENTOR_DB_NAME: "REPLACE_VIA_VAULT"
  AI_MENTOR_DB_USER: "REPLACE_VIA_VAULT"
  AI_MENTOR_DB_PASSWORD: "REPLACE_VIA_VAULT"

  CAREER_ORACLE_DB_NAME: "REPLACE_VIA_VAULT"
  CAREER_ORACLE_DB_USER: "REPLACE_VIA_VAULT"
  CAREER_ORACLE_DB_PASSWORD: "REPLACE_VIA_VAULT"

  MENTOR_DB_NAME: "REPLACE_VIA_VAULT"
  MENTOR_DB_USER: "REPLACE_VIA_VAULT"
  MENTOR_DB_PASSWORD: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Redis
  # -------------------------------------------------------------------------
  REDIS_PASSWORD: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # JWT (RS256 asymmetric keys)
  # -------------------------------------------------------------------------
  JWT_PRIVATE_KEY_PATH: "REPLACE_VIA_VAULT"
  JWT_PUBLIC_KEY_PATH: "REPLACE_VIA_VAULT"
  JWT_ISSUER: "REPLACE_VIA_VAULT"
  JWT_ACCESS_TOKEN_EXPIRY_SECONDS: "REPLACE_VIA_VAULT"
  JWT_REFRESH_TOKEN_EXPIRY_SECONDS: "REPLACE_VIA_VAULT"
  JWT_JWKS_URI: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Argon2id password hashing (OWASP 2024 recommended)
  # -------------------------------------------------------------------------
  ARGON2_MEMORY_COST: "REPLACE_VIA_VAULT"
  ARGON2_ITERATIONS: "REPLACE_VIA_VAULT"
  ARGON2_PARALLELISM: "REPLACE_VIA_VAULT"
  ARGON2_SALT_LENGTH: "REPLACE_VIA_VAULT"
  ARGON2_HASH_LENGTH: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # OTP (TOTP — RFC 6238)
  # -------------------------------------------------------------------------
  OTP_EXPIRY_SECONDS: "REPLACE_VIA_VAULT"
  OTP_MAX_ATTEMPTS: "REPLACE_VIA_VAULT"
  OTP_LENGTH: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # hCaptcha / reCAPTCHA
  # -------------------------------------------------------------------------
  HCAPTCHA_VERIFY_URL: "REPLACE_VIA_VAULT"
  HCAPTCHA_SITE_KEY: "REPLACE_VIA_VAULT"
  HCAPTCHA_SECRET_KEY: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Email (SMTP)
  # -------------------------------------------------------------------------
  MAIL_USERNAME: "REPLACE_VIA_VAULT"
  MAIL_PASSWORD: "REPLACE_VIA_VAULT"
  MAIL_FROM_ADDRESS: "REPLACE_VIA_VAULT"
  MAIL_FROM_NAME: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # SMS / Twilio
  # -------------------------------------------------------------------------
  TWILIO_ACCOUNT_SID: "REPLACE_VIA_VAULT"
  TWILIO_AUTH_TOKEN: "REPLACE_VIA_VAULT"
  TWILIO_VERIFY_SERVICE_SID: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Firebase (push notifications)
  # -------------------------------------------------------------------------
  FIREBASE_PROJECT_ID: "REPLACE_VIA_VAULT"
  FIREBASE_SERVICE_ACCOUNT_KEY_PATH: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # AI / LLM
  # -------------------------------------------------------------------------
  ANTHROPIC_API_KEY: "REPLACE_VIA_VAULT"
  ANTHROPIC_MODEL: "REPLACE_VIA_VAULT"
  ANTHROPIC_BASE_URL: "REPLACE_VIA_VAULT"
  OPENAI_API_KEY: "REPLACE_VIA_VAULT"
  OLLAMA_BASE_URL: "REPLACE_VIA_VAULT"
  AI_EMBEDDING_MODEL: "REPLACE_VIA_VAULT"
  AI_EMBEDDING_DIMENSIONS: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Kafka topic names
  # -------------------------------------------------------------------------
  KAFKA_TOPIC_AUTH_EVENTS: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_AUDIT_IMMUTABLE: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_NOTIFICATION_SEND: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_PSYCH_EVENTS: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_ASSESS_EVENTS: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_CENTER_EVENTS: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_PARENT_EVENTS: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_AI_MENTOR_STUDY_PLAN_CREATED: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_AI_MENTOR_DOUBT_SUBMITTED: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_AI_MENTOR_DOUBT_RESOLVED: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_CAREER_ORACLE_RECOMMENDED: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_CAREER_ORACLE_PROFILE_CREATED: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_MENTOR_SESSION_BOOKED: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_MENTOR_SESSION_COMPLETED: "REPLACE_VIA_VAULT"
  KAFKA_TOPIC_MENTOR_SESSION_FEEDBACK: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Kafka consumer group IDs
  # -------------------------------------------------------------------------
  AUTH_SVC_KAFKA_CONSUMER_GROUP: "REPLACE_VIA_VAULT"
  PARENT_SVC_KAFKA_CONSUMER_GROUP: "REPLACE_VIA_VAULT"
  CENTER_SVC_KAFKA_CONSUMER_GROUP: "REPLACE_VIA_VAULT"
  ASSESS_SVC_KAFKA_CONSUMER_GROUP: "REPLACE_VIA_VAULT"
  PSYCH_SVC_KAFKA_CONSUMER_GROUP: "REPLACE_VIA_VAULT"
  AI_GATEWAY_SVC_KAFKA_CONSUMER_GROUP: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Resilience4j — circuit breaker + rate limiter
  # -------------------------------------------------------------------------
  R4J_CB_AI_WINDOW_SIZE: "REPLACE_VIA_VAULT"
  R4J_CB_AI_FAILURE_THRESHOLD: "REPLACE_VIA_VAULT"
  R4J_CB_AI_WAIT_DURATION: "REPLACE_VIA_VAULT"
  R4J_CB_AUTH_WINDOW_SIZE: "REPLACE_VIA_VAULT"
  R4J_CB_AUTH_FAILURE_THRESHOLD: "REPLACE_VIA_VAULT"
  R4J_CB_AUTH_WAIT_DURATION: "REPLACE_VIA_VAULT"
  R4J_CB_CAPTCHA_WINDOW_SIZE: "REPLACE_VIA_VAULT"
  R4J_CB_CAPTCHA_FAILURE_THRESHOLD: "REPLACE_VIA_VAULT"
  R4J_CB_CAPTCHA_WAIT_DURATION: "REPLACE_VIA_VAULT"
  R4J_TL_AI_TIMEOUT: "REPLACE_VIA_VAULT"
  R4J_RL_LOGIN_LIMIT: "REPLACE_VIA_VAULT"
  R4J_RL_LOGIN_REFRESH_PERIOD: "REPLACE_VIA_VAULT"
  R4J_RL_LOGIN_TIMEOUT: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # API Gateway rate limiter
  # -------------------------------------------------------------------------
  GATEWAY_RATE_LIMIT_REPLENISH_RATE: "REPLACE_VIA_VAULT"
  GATEWAY_RATE_LIMIT_BURST_CAPACITY: "REPLACE_VIA_VAULT"
  GATEWAY_RATE_LIMIT_REQUESTED_TOKENS: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # Python AI sidecar timeouts
  # -------------------------------------------------------------------------
  PSYCH_AI_SVC_CONNECT_TIMEOUT_SECONDS: "REPLACE_VIA_VAULT"
  PSYCH_AI_SVC_READ_TIMEOUT_SECONDS: "REPLACE_VIA_VAULT"

  # -------------------------------------------------------------------------
  # assess-svc WebSocket relay (STOMP)
  # -------------------------------------------------------------------------
  ASSESS_SVC_WS_DESTINATION_PREFIX: "REPLACE_VIA_VAULT"
  ASSESS_SVC_WS_RELAY_HOST: "REPLACE_VIA_VAULT"
  ASSESS_SVC_WS_RELAY_PORT: "REPLACE_VIA_VAULT"
