apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "edutech.labels" . | nindent 4 }}
    {{ include "edutech.selectorLabels" (dict "app" "auth-svc") | nindent 4 }}
spec:
  selector:
    {{ include "edutech.selectorLabels" (dict "app" "auth-svc") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.authSvc.port }}
      targetPort: {{ .Values.services.authSvc.port }}
      protocol: TCP
  type: ClusterIP
