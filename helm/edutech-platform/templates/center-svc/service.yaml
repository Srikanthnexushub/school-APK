apiVersion: v1
kind: Service
metadata:
  name: center-svc
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "edutech.labels" . | nindent 4 }}
    {{ include "edutech.selectorLabels" (dict "app" "center-svc") | nindent 4 }}
spec:
  selector:
    {{ include "edutech.selectorLabels" (dict "app" "center-svc") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.centerSvc.port }}
      targetPort: {{ .Values.services.centerSvc.port }}
      protocol: TCP
  type: ClusterIP
