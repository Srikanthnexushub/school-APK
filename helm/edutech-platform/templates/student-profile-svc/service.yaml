apiVersion: v1
kind: Service
metadata:
  name: student-profile-svc
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "edutech.labels" . | nindent 4 }}
    {{ include "edutech.selectorLabels" (dict "app" "student-profile-svc") | nindent 4 }}
spec:
  selector:
    {{ include "edutech.selectorLabels" (dict "app" "student-profile-svc") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.studentProfileSvc.port }}
      targetPort: {{ .Values.services.studentProfileSvc.port }}
      protocol: TCP
  type: ClusterIP
