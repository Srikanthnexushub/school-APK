apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-profile-svc
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "edutech.labels" . | nindent 4 }}
    {{ include "edutech.selectorLabels" (dict "app" "student-profile-svc") | nindent 4 }}
spec:
  replicas: {{ .Values.services.studentProfileSvc.replicas }}
  selector:
    matchLabels:
      {{ include "edutech.selectorLabels" (dict "app" "student-profile-svc") | nindent 6 }}
  template:
    metadata:
      labels:
        {{ include "edutech.selectorLabels" (dict "app" "student-profile-svc") | nindent 8 }}
        app.kubernetes.io/part-of: edutech-platform
      annotations:
        {{ include "edutech.prometheusAnnotations" (dict "port" (toString .Values.services.studentProfileSvc.port)) | nindent 8 }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      securityContext:
        {{ include "edutech.podSecurityContext" . | nindent 8 }}
      {{ include "edutech.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: student-profile-svc
          image: {{ include "edutech.image" (dict "name" "student-profile-svc" "Values" .Values) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.studentProfileSvc.port }}
              name: http
              protocol: TCP
          {{ include "edutech.envFrom" . | nindent 10 }}
          env:
            - name: SERVER_PORT
              value: {{ .Values.services.studentProfileSvc.port | quote }}
            - name: STUDENT_PROFILE_SVC_PORT
              value: {{ .Values.services.studentProfileSvc.port | quote }}
          {{ include "edutech.readinessProbe" (dict "port" .Values.services.studentProfileSvc.port) | nindent 10 }}
          {{ include "edutech.livenessProbe" (dict "port" .Values.services.studentProfileSvc.port) | nindent 10 }}
          resources:
            {{- toYaml .Values.services.studentProfileSvc.resources | nindent 12 }}
          securityContext:
            {{ include "edutech.containerSecurityContext" . | nindent 12 }}
