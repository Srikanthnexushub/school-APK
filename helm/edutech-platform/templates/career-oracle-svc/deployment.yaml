apiVersion: apps/v1
kind: Deployment
metadata:
  name: career-oracle-svc
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "edutech.labels" . | nindent 4 }}
    {{ include "edutech.selectorLabels" (dict "app" "career-oracle-svc") | nindent 4 }}
spec:
  replicas: {{ .Values.services.careerOracleSvc.replicas }}
  selector:
    matchLabels:
      {{ include "edutech.selectorLabels" (dict "app" "career-oracle-svc") | nindent 6 }}
  template:
    metadata:
      labels:
        {{ include "edutech.selectorLabels" (dict "app" "career-oracle-svc") | nindent 8 }}
        app.kubernetes.io/part-of: edutech-platform
      annotations:
        {{ include "edutech.prometheusAnnotations" (dict "port" (toString .Values.services.careerOracleSvc.port)) | nindent 8 }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      securityContext:
        {{ include "edutech.podSecurityContext" . | nindent 8 }}
      {{ include "edutech.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: career-oracle-svc
          image: {{ include "edutech.image" (dict "name" "career-oracle-svc" "Values" .Values) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.careerOracleSvc.port }}
              name: http
              protocol: TCP
            - containerPort: {{ .Values.services.careerOracleSvc.grpcPort }}
              name: grpc
              protocol: TCP
          {{ include "edutech.envFrom" . | nindent 10 }}
          env:
            - name: SERVER_PORT
              value: {{ .Values.services.careerOracleSvc.port | quote }}
            - name: CAREER_ORACLE_SVC_PORT
              value: {{ .Values.services.careerOracleSvc.port | quote }}
          {{ include "edutech.readinessProbe" (dict "port" .Values.services.careerOracleSvc.port) | nindent 10 }}
          {{ include "edutech.livenessProbe" (dict "port" .Values.services.careerOracleSvc.port) | nindent 10 }}
          resources:
            {{- toYaml .Values.services.careerOracleSvc.resources | nindent 12 }}
          securityContext:
            {{ include "edutech.containerSecurityContext" . | nindent 12 }}
