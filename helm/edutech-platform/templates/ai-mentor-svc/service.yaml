apiVersion: v1
kind: Service
metadata:
  name: ai-mentor-svc
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "edutech.labels" . | nindent 4 }}
    {{ include "edutech.selectorLabels" (dict "app" "ai-mentor-svc") | nindent 4 }}
spec:
  selector:
    {{ include "edutech.selectorLabels" (dict "app" "ai-mentor-svc") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.aiMentorSvc.port }}
      targetPort: {{ .Values.services.aiMentorSvc.port }}
      protocol: TCP
    - name: grpc
      port: {{ .Values.services.aiMentorSvc.grpcPort }}
      targetPort: {{ .Values.services.aiMentorSvc.grpcPort }}
      protocol: TCP
  type: ClusterIP
