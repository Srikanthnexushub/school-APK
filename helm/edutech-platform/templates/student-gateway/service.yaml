apiVersion: v1
kind: Service
metadata:
  name: student-gateway
  namespace: {{ .Values.namespace }}
  labels:
    {{ include "edutech.labels" . | nindent 4 }}
    {{ include "edutech.selectorLabels" (dict "app" "student-gateway") | nindent 4 }}
spec:
  selector:
    {{ include "edutech.selectorLabels" (dict "app" "student-gateway") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.studentGateway.port }}
      targetPort: {{ .Values.services.studentGateway.port }}
      protocol: TCP
  type: ClusterIP
